load("@tx-kit-ext//rules/build:tx_binary.bzl", "tx_binary")

tx_binary(
    name = "try-asio",
    srcs = glob(["*.cpp"]),
    deps = [
        "@tx-pkg-aux//:pkg-boot",
        "@asio//:asio",
    ],
)
