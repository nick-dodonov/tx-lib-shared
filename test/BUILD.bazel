load("@tx-kit-ext//rules/build:tx_test.bzl", "tx_test")

tx_test(
    name = "misc",
    srcs = glob(["*.cpp", "*.h"]),
    defines = ["ASIO_HAS_PTHREADS=1"],
    deps = [
        "@googletest//:gtest_main",
        "@tx-pkg-aux//src:pkg-log",
        "@asio//:asio",
    ],
)
